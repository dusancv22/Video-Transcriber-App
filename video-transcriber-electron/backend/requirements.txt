# FastAPI Backend Requirements
fastapi==0.104.1
uvicorn[standard]==0.24.0
websockets==12.0
pydantic==2.5.0
python-multipart==0.0.6

# Use existing requirements from parent directory
# The main application dependencies are already installed in the parent venv